[project]
name = "site-monitor-cloud"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.0",
    "gunicorn>=23.0.0",
    "pymongo>=4.13.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]
